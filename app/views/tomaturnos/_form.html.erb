<%= form_for(@tomaturno) do |f| %>
  <% if @tomaturno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tomaturno.errors.count, "errores") %> prohibited this tomaturno from being saved:</h2>

      <ul>
      <% @tomaturno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :canmaxsemana,'Cantidad de Turnos por Empaque' %><br>
    <%= f.number_field :canmaxsemana, min:"1",max:"15" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
